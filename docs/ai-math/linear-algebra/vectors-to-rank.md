# 一、从向量到秩

## 1. 向量：不只是「箭头」

### 1.1 向量的三种面目

同一个向量 $v = \begin{bmatrix} 3 \\ 2 \end{bmatrix}$，有三种理解方式：

| 视角 | $v$ 是什么 | 什么时候用 |
|------|-----------|-----------|
| **几何** | 从原点指向 (3, 2) 的箭头 | 建立直觉，理解「方向」和「长度」 |
| **代数** | 一列数 $(3, 2)^T$ | 写代码、推公式 |
| **数据** | 一个有两个特征的样本 | 机器学习里的实际含义 |

在 ML 里，数据视角才是主角。一个 768 维的词向量不是你能画出来的箭头，但它和二维箭头遵守完全相同的规则。

### 1.2 向量的基本运算

**加法**：$\begin{bmatrix} 1 \\ 2 \end{bmatrix} + \begin{bmatrix} 3 \\ 1 \end{bmatrix} = \begin{bmatrix} 4 \\ 3 \end{bmatrix}$

几何上就是拼在一起——先走第一个箭头，再走第二个。

**数乘**：$2 \cdot \begin{bmatrix} 1 \\ 2 \end{bmatrix} = \begin{bmatrix} 2 \\ 4 \end{bmatrix}$

拉伸（或缩短）箭头，方向不变（负数会反向）。

**内积**：$\begin{bmatrix} 1 \\ 2 \end{bmatrix} \cdot \begin{bmatrix} 3 \\ 4 \end{bmatrix} = 1 \times 3 + 2 \times 4 = 11$

> 💡 **几何直觉**：内积 = 一个向量在另一个向量方向上的投影长度 × 另一个向量的长度。内积为 0 意味着两个向量垂直。Attention 里的 $QK^T$ 就是算一堆向量两两之间的内积。

---

## 2. 线性组合：万物的起点

取两个向量 $v_1, v_2$ 和两个数 $c_1, c_2$，构造
$$c_1 v_1 + c_2 v_2$$

这就是**线性组合**。它回答一个核心问题：**用这几个向量，能「够到」哪些地方？**

来，看三个具体的例子：

**例 1**：$v_1 = \begin{bmatrix} 1 \\ 0 \end{bmatrix}$，$v_2 = \begin{bmatrix} 0 \\ 1 \end{bmatrix}$

$$\text{所有 }c_1 v_1 + c_2 v_2 = \begin{bmatrix} c_1 \\ c_2 \end{bmatrix}$$

能填满整个平面。因为两个向量指向两个独立的方向（不共线），搭配不同的系数能到达平面上的任何一点。

**例 2**：$v_1 = \begin{bmatrix} 1 \\ 2 \end{bmatrix}$，$v_2 = \begin{bmatrix} 2 \\ 4 \end{bmatrix}$

$v_2$ 就是 $2v_1$，所以 $c_1 v_1 + c_2 v_2 = (c_1 + 2c_2) v_1$——所有线性组合都落在 $v_1$ 所在的那条直线上。**只能到达一条线。**

**例 3**：$v_1 = \begin{bmatrix} 0 \\ 0 \end{bmatrix}$，$v_2 = \begin{bmatrix} 1 \\ 0 \end{bmatrix}$

线性组合 $c_1 \cdot 0 + c_2 v_2 = c_2 v_2$，只能到达 $x$ 轴——也是一条线，只不过被零向量「浪费」了一个位置。

> 🔑 这三个例子其实在教同一件事：**线性组合能到达的范围，取决于这些向量中有多少个「真正独立」的方向。** 这个「独立方向的个数」，就是接下来要说的——**秩**。

> 💡 **在 ML 里**：神经网络的前向传播 $Wx$ 就是 $W$ 各列以 $x$ 各分量为系数的线性组合。你喂进去的 $x$ 选定了 $W$ 各列的「混合配方」。

---

## 3. 矩阵：线性变换的「配方」

### 3.1 什么是线性变换

一个变换 $T$ 是**线性的**，当它满足两条：

1. $T(u + v) = T(u) + T(v)$ —— 和的变换 = 变换的和
2. $T(cv) = c T(v)$ —— 缩放的变换 = 变换的缩放

换句话说：**线性变换保持「线性组合的结构」不变**。先组合再变换，等于先变换再组合。

### 3.2 矩阵就是线性变换的「说明书」

任何线性变换都可以用一个矩阵来表示。矩阵的**列**告诉你：原空间的标准基向量 $\hat{i}, \hat{j}$ 被变换到哪里去了。

例如矩阵 $A = \begin{bmatrix} 2 & -1 \\ 1 & 1 \end{bmatrix}$：
- 第一列 $\begin{bmatrix} 2 \\ 1 \end{bmatrix}$ 是 $\hat{i}$ 的去向
- 第二列 $\begin{bmatrix} -1 \\ 1 \end{bmatrix}$ 是 $\hat{j}$ 的去向

有了这两个去向，整个空间的变换就唯一确定了——因为其他向量都是 $\hat{i}$ 和 $\hat{j}$ 的线性组合。

这就是理解矩阵乘法的**列图景**：$Ax$ 是用 $x$ 的各分量作为系数，对 $A$ 的各列做线性组合。不要死记「行乘列」的规则，先在脑子里看到这个过程。

### 3.3 矩阵乘法的四种理解

同一个 $C = AB$，有四种等价的理解方式：

| 视角 | 公式 | 直觉 |
|------|------|------|
| **列组合** | $C$ 第 $j$ 列 = $A \times (B$ 第 $j$ 列$)$ | 先对 $B$ 的列做线性组合，得到 $A$ 各列的新配方 |
| **行组合** | $C$ 第 $i$ 行 = $(A$ 第 $i$ 行$) \times B$ | $A$ 的行作为系数，组合 $B$ 的行 |
| **外积和** | $AB = \sum_k (A$ 第 $k$ 列$) \times (B$ 第 $k$ 行$)$ | 拆成一堆秩-1 矩阵的和，揭示矩阵的「骨架」 |
| **纯粹计算** | $C_{ij} = \sum_k A_{ik} B_{kj}$ | 写代码用的，不是用来理解的 |

---

## 4. 线性无关、基与维数

在进入「秩」之前，需要三块积木。它们不是抽象定义，而是前面那个「能到达哪些地方」问题的精确表述。

### 4.1 线性无关：你中有我吗？

一组向量 $\{v_1, v_2, \dots, v_k\}$ 是**线性无关**的，当**唯一**让下面等式成立的系数是全零：

$$c_1 v_1 + c_2 v_2 + \dots + c_k v_k = 0 \quad \Longrightarrow \quad c_1 = c_2 = \dots = c_k = 0$$

用人话说：**这组向量里没有任何一个可以被其他几个线性组合出来。** 每个向量都贡献了新的「方向信息」。

如果存在非全零的系数让等式成立，这组向量就是**线性相关**的——至少有一个向量是「多余的」，它的信息已经被其他向量覆盖了。

**直观检验法**（二维/三维好用，高维靠计算）：

| 向量个数 | 无关的条件 | 相关的标志 |
|---------|-----------|-----------|
| 2 个 | 不在同一条直线上（不共线） | 一个恰好是另一个的倍数 |
| 3 个 | 不在同一个平面上 | 至少一个能用另外两个拼出来 |
| 4+ 个（在 $\mathbb{R}^3$ 中） | **不可能！** 在三维空间里最多只有 3 个线性无关的向量 | 必然相关 |

### 4.2 基：最少的力量，最大的覆盖

一个向量空间的**基**是一组满足两个条件的向量：

1. **线性无关**：没有多余的人
2. **张成整个空间**：用它们的线性组合能到达空间里的每一点

$\mathbb{R}^2$ 的标准基：$\left\{\begin{bmatrix} 1 \\ 0 \end{bmatrix}, \begin{bmatrix} 0 \\ 1 \end{bmatrix}\right\}$

$\mathbb{R}^3$ 的标准基：$\left\{\begin{bmatrix} 1 \\ 0 \\ 0 \end{bmatrix}, \begin{bmatrix} 0 \\ 1 \\ 0 \end{bmatrix}, \begin{bmatrix} 0 \\ 0 \\ 1 \end{bmatrix}\right\}$

**基不唯一**。$\left\{\begin{bmatrix} 1 \\ 1 \end{bmatrix}, \begin{bmatrix} 1 \\ -1 \end{bmatrix}\right\}$ 也是 $\mathbb{R}^2$ 的一组基——只要两个向量不共线就行。但任何基里的向量个数是固定的——这就是维数。

### 4.3 维数：空间需要几个「坐标轴」

一个向量空间的**维数** = 它的任何一组基中的向量个数。

- $\mathbb{R}^1$（一条线）：维数 = 1
- $\mathbb{R}^2$（一个平面）：维数 = 2
- $\mathbb{R}^n$：维数 = $n$

**子空间的维数可以小于整个空间的维数。** 在 $\mathbb{R}^3$ 中，一个平面是 2 维子空间，一条线是 1 维子空间，一个点是 0 维子空间。

> 🔑 这三块积木——无关、基、维数——组装在一起，就是我们接下来要讲的**秩**。

---

## 5. 秩：矩阵的「有效维度」

这是线性代数里最重要的一个数。它回答一个朴素的问题：**这个矩阵到底在几个方向上「真正起作用」？**

### 5.1 定义：秩是什么

矩阵 $A$ 的**秩** = $A$ 的列空间中线性无关的列的最大个数

用我们前面学的语言：**秩 = 列空间的维数**。也就是 $A$ 的列能「张出」几维的空间。

等价定义（同样重要）：秩 = $A$ 的行空间中线性无关的行的最大个数。**行秩永远等于列秩**——这就是「秩」不需要区分「行秩」「列秩」的原因。

### 5.2 三个例子让你彻底理解

**例 1：满秩矩阵**

$$A = \begin{bmatrix} 1 & 2 \\ 3 & 4 \end{bmatrix}, \quad \text{秩}(A) = 2$$

两列不共线 → 线性无关 → 列空间是整个 $\mathbb{R}^2$（2 维）。它的秩等于列数（也是行数），这叫**满秩**。

作用：$A$ 把输入空间「完整地」映射到 2 维输出——没有信息丢失，可逆。

**例 2：秩亏矩阵**

$$B = \begin{bmatrix} 1 & 2 \\ 2 & 4 \end{bmatrix}, \quad \text{秩}(B) = 1$$

第二列是第一列的 2 倍 → 两列共线 → 只有一个独立方向。列空间是一条线（1 维）。

作用：$B$ 把整个二维输入**全部压扁到一条线上**。不管输入什么，输出永远落在那条线上。**不可逆**——你无法从线上的一个点恢复原始的二维输入，因为无数个输入会映射到同一个输出。

**例 3：零秩矩阵**

$$C = \begin{bmatrix} 0 & 0 \\ 0 & 0 \end{bmatrix}, \quad \text{秩}(C) = 0$$

全部列都是零向量 → 没有独立方向 → 列空间只是一个点 $\{0\}$。

作用：$C$ 把整个输入空间**压扁到一个点**。

### 5.3 怎么求秩：行化简法

你不需要猜哪些列是独立的。一个可靠的方法：**把矩阵化简为行阶梯形，数一数有多少个主元。**

原始矩阵：
$$\begin{bmatrix} 1 & 2 & 3 \\ 2 & 4 & 6 \\ 1 & 3 & 4 \end{bmatrix}$$

消元过程（第 2 行减 2 倍第 1 行；第 3 行减第 1 行）：
$$\rightarrow \begin{bmatrix} 1 & 2 & 3 \\ 0 & 0 & 0 \\ 0 & 1 & 1 \end{bmatrix}$$

交换第 2、3 行：
$$\rightarrow \begin{bmatrix} 1 & 2 & 3 \\ 0 & 1 & 1 \\ 0 & 0 & 0 \end{bmatrix}$$

行阶梯形，有 2 个非零行 → **秩 = 2**。

> 行化简求秩的核心：把矩阵往下消，剩几个「站着的行」就是几。

### 5.4 秩告诉你什么：一张表秒懂

对任意 $m \times n$ 矩阵 $A$，记秩为 $r$：

| $r$ 的值 | 意味着什么 | 矩阵「健康程度」 |
|---------|-----------|----------------|
| $r = m = n$ | 满秩方阵，列空间 = 整个 $\mathbb{R}^n$ | 完美——可逆，变换是「完整」的 |
| $r < \min(m, n)$ | 秩亏——至少有一个「被浪费的方向」 | 信息丢失——多种输入映射到同一输出 |
| $r = 0$ | 零矩阵 | 一切都被压扁到原点 |

> **方阵可逆 $\iff$ 满秩 $\iff \det(A) \neq 0$。** 长方形矩阵不谈「可逆」，但秩同样告诉你它的「有效维度」。

### 5.5 秩在 ML 里无处不在

| 场景 | 秩是什么角色 |
|------|------------|
| **数据矩阵**（样本 × 特征） | 秩 = 数据的「内在维度」。1000 维特征，但秩只有 20 → 数据实际上活在 20 维的子空间里 |
| **LoRA** | $r$ 就是秩。$r$ 越小，可调参数越少，但模型表达能力也越受限。调参的核心就是找一个平衡 |
| **Attention 矩阵** | 理论上可以满秩，但实际往往是低秩的——很多 token 之间的注意力分布大同小异 |
| **梯度矩阵** | 低秩梯度意味着大部分参数更新方向高度相关——优化在低维流形上进行 |

---

> **下一步**：[二、方程与子空间](elimination-and-subspaces.md) — 有了秩，我们来看看它如何决定 $Ax=b$ 的解。
